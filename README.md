# ani-ftune

## Overview

## Installation

For developers:

```bash
bash ./make_env.sh
# or conda|mamba env create --file ./environment.yaml, and manually check dependencies
# of the submodules
pip install -e .
```
